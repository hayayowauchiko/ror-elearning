<% unless current_user?(@user) %>
    <div id="follow_form">
        <% if current_user.following?(@user) %>
            <%= render 'unfollow' %>
        <% else %>
            <%= render 'follow' %>
        <% end %>
    </div>
<% end %>
<!-- なぜ上記のrenderが/usersを省略できるかというと、全てのファイルがsharedフォルダ内にあるから -->